#!/usr/bin/python3

# Guardar como archivo .py en la carpeta que tenemos con git
# Guardar como archivo"ejemplo_cilclo.py"
# Los espacios si importan
i = 6
 while i > 0 :
 		print(i)
 			i = i - 1 

ciudades = ["Arica", "Iquique", "Amtofagasta", "Copiapo"]
for ciudad in ciudades:
		print(ciudad)